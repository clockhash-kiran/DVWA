name: Pre Phase Scans

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  mid-phase:
    name: Pre Phase
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Pre Phase Security Scan
        uses: clockhash-kiran/scan-actions/pre-phase@main
        with:
          phase: pre
          target_url: "github.com/digininja/DVWA"
          user_id: "mnKgvDd9StJRpKSZqkW1XKucZOrbOU44"
          project_id: "w8dQJCvRlQEdPiXE8cDYg"
          api_token: ${{ secrets.API_TOKEN }}
          branch: "master"
